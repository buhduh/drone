/*
#include <stdlib.h>

#include "memory/heap_allocator.hpp"
uses the heap in the standard way, eg malloc and free

namespace memory {

HeapAllocator::HeapAllocator()
{
}

HeapAllocator::~HeapAllocator() {
}

//template Block<int> HeapAllocator::allocate<int>(size_t);
//template void HeapAllocator::deallocate<int>(Block<int>&);

}
*/
